// 배포 환경 설정 — 프런트가 어느 API 를 볼지 정한다(코드 수정 없이 배포별로 교체).
//
// 이 파일은 **통합 배포(112, FastAPI 가 UI 를 함께 서빙)** 용 기본값이다.
// apiBase 가 비어 있으면 fetch 가 상대경로 그대로 나가 same-origin 으로 동작한다.
//
// 프런트를 별도 서버(IIS 등)로 분리 배포할 때는 deployment/build_frontend.py 가
// 이 파일을 API 주소가 채워진 것으로 바꿔 넣는다. 앱 코드는 손대지 않는다 —
// fetch 27곳이 전부 상대경로라 래퍼 한 곳에서 베이스만 앞에 붙이면 되기 때문이다.
window.NOVA_CONFIG = {
  // 예: "http://192.168.21.112:8600" (끝 슬래시 없이). 빈 문자열이면 same-origin.
  apiBase: "",
  // 테넌트 API 키. ★브라우저로 내려가는 값이라 이 페이지에 닿는 사람은 모두 볼 수 있다.
  //   분리 배포 시 노출 범위가 넓어지므로, 공개 구간에 둘 거라면 별도 대책이 필요하다.
  apiKey: "nexus-b200-test-key-001",
};
