# ── LLM 추론 ──
torch>=2.3.0
transformers>=4.44.0
accelerate>=0.33.0
safetensors>=0.4.0
vllm>=0.5.0; sys_platform == "linux"

# ── API & Web ──
fastapi>=0.112.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
pydantic-settings>=2.4.0
sse-starlette>=2.1.0

# ── 데이터베이스 ──
asyncpg>=0.29.0
aiosqlite>=0.20.0
redis[hiredis]>=5.0.0
pgvector>=0.3.0

# ── 임베딩 ──
sentence-transformers>=3.0.0

# ── CLI ──
rich>=13.7.0
prompt-toolkit>=3.0.47
click>=8.1.7

# ── 문서 파싱/생성 ──
# DocumentProcess(업로드 문서 읽기) · DocumentExport(문서 생성) · core/ingest 파서가
# 사용한다. 전부 순수 파이썬이라 에어갭 오프라인 wheel 번들에 그대로 담긴다.
# (.hwp 구포맷만 예외 — LibreOffice(soffice) 바이너리가 별도로 필요하다.)
pypdf>=4.2.0
pdfplumber>=0.11.0
python-docx>=1.1.0
python-pptx>=0.6.23
openpyxl>=3.1.0
python-hwpx>=2.24.0
defusedxml>=0.7.1
olefile>=0.47          # HWP 5.0(.hwp) OLE2 복합문서 읽기 — HwpNativeParser

# ── 유틸리티 ──
pyyaml>=6.0.1
jinja2>=3.1.4
aiofiles>=24.1.0
structlog>=24.4.0
psutil>=6.0.0
watchdog>=4.0.0
orjson>=3.10.0
httpx>=0.27.0
