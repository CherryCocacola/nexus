# Project Nexus 진행 기록

작업 계획과 결정의 이유를 여기에 누적한다. 다음 세션이 다시 유도하지 않아도 되도록,
**무엇을 왜 그렇게 했는지**와 **아직 검증되지 않은 것**을 함께 남긴다.

기록 시작 2026-08-25. 그 이전 이력은 git 로그와 `user_mig/` 문서에 있다.

---

## 2026-08-25 ~ 08-26 · VSCode 플러그인 회귀 측정 중 발견한 서버 결함 3건

플러그인 팀의 자동 회귀 측정(42 Case × N회)에서 드러난 것들이다. 세 건 모두
NOVA 서버 쪽 원인이었고 클라이언트가 우회할 수 없는 종류였다.

### 결함 1 · 압축 상태가 요청 간에 새어 입력을 잘라먹었다 (최우선)

**증상.** 같은 요청인데 `prompt_tokens` 가 입력 크기와 무관한 고정값이 됐다.
입력이 5,282자든 81,077자든 같은 값이 나왔다. 즉 사용자 메시지가 모델에
도달하지 않았다.

```
08-20        4,674 고정
08-24 03:07  압축 → 이후 모든 요청 +138 토큰 고정   (요약만 남은 가벼운 형태)
08-25 08:24  압축 → 이후 5,504·4,688 고정          (경계까지 남아 입력 유실)
```

**원인.** 웹은 요청마다 세션 전용 엔진을 조립하면서 `ContextManager` 는 기동 시
만든 하나를 공유했다(`web/app.py` 의 `parts["context_manager"]`). 그런데 이 객체에는
대화별 상태가 있다.

```python
active = messages[self._compact_boundary :]                  # 경계 이전을 버린다
if self._compact_summary: result.append(Message.system(...))  # 요약을 앞에 붙인다
```

어떤 요청 하나가 auto-compact 를 유발하면 그 경계가 인스턴스에 남고, **이후 모든
요청**이 그 경계로 잘렸다. 플러그인 요청은 메시지가 1~2개뿐이라 통째로 사라졌다.

**왜 추적이 오래 걸렸나.** 재기동하면 인스턴스가 새로 생겨 증상이 사라진다.
그리고 남은 값(경계·요약)에 따라 고정값이 매번 달라져 클라이언트가 상수로 막을 수
없었다. 08-24 의 +138 은 요청 본문이 바이트 동일한데 프리앰블만 고정폭으로 늘어
일주일을 헤맸는데, 같은 버그의 가벼운 형태였다.

**결정적 증거.** 보존해 둔 구 컨테이너 로그에서 `08-24 03:07:56 Auto-compact` 를
찾았다. +138 전환 관측 구간이 `03:07:40 ~ 03:11:59` 였고, 그날 유일한 압축이
정확히 그 안이었다.

**수정** — 커밋 `4c53966`
- `ContextManager.for_session()` 으로 요청별 인스턴스를 만든다. 설정은 복제하고
  압축 상태만 초기화한다. 설정의 단일 출처는 bootstrap 이 만든 원본 하나로 둔다
  — 호출부가 인자를 다시 조립하면 두 벌이 되어 언젠가 어긋난다.
- CLI 는 프로세스당 세션이 하나라 공유가 곧 격리다. 건드리지 않았다(무회귀).
- 압축 상태를 **결과 검증 후에만** 커밋한다. 이전에는 검증 전에 써서, 요약이
  원본보다 큰 경우에도 경계가 남았다(`08-24 -262`, `08-25 -333`).
  `force=True` 는 제외했다 — 에러 복구 경로라 원본을 돌려주면 호출자가 같은
  초과로 다시 실패한다. 실측 사고 2건은 모두 `force=False` 였다.

> **★ 아직 실전 검증 전이다.** 배포 후 9시간 회차에서 auto-compact 이 0건이었다.
> 무장애였지만 트리거가 없었으므로 "수정 덕분"이라고 단정할 수 없다. 다음 회차에서
> 압축이 발생하면 그때 확정된다. 로그에 아래가 찍히면 정상이다.
>
> ```
> Auto-compact 완료: N → M (절약: 양수)      그 요청에만 적용
> Auto-compact 무효 — 결과가 더 크거나 같다   이득 없어 상태 미커밋
> ```

### 결함 2 · 서버가 자기 JSON 을 깨뜨렸다

**증상.** 구조화 출력(guided JSON) 요청에서 `INVALID_STRUCTURED_OUTPUT` 32건
(19,620 요청 중 0.16%). 전부 `finish_reason=stop`.

**원인.** 모델은 유효한 JSON 을 냈다. web 계층이 유효성 검사 **직전에** 사후 검증
경고를 마크다운으로 본문에 이어 붙였고, 그 결과 JSON 이 깨졌다. 그리고 그 깨진
결과를 바로 다음 줄에서 다시 검사해 "유효한 JSON 이 아닙니다"로 판정했다.

```
모델 출력            {"type":"chat_response","content":[...]}   파싱 성공
+ 실행주장 경고 160자  \n---\n⚠️ **실행 확인 필요** …
= 검사 결과          Extra data: line 10 column 1               파싱 실패
```

실행 주장 검증기가 발화한 이유는 이 표면 특성이다 — 도구를 **클라이언트가**
실행하므로 서버 메시지에 `role="tool_result"` 가 없고, 검증기는 "실행했다면서
도구 기록이 없다"로 읽는다.

**수정** — 커밋 `5aeb524`. 구조화 출력에서는 본문에 아무것도 덧붙이지 않는다.
신호는 버리지 않고 `warnings` 배열로 옮긴다. 다운로드 링크도 같은 이유로 본문
부착을 멈췄다(`downloads` 필드로 이미 구조화되어 나가므로 정보 손실 없음).
평문 응답의 동작은 그대로다.

### 결함 3 · 근거 없는 "적용했다" 주장이 조용히 통과했다

**증상.** 모델이 "변경이 성공적으로 적용되었습니다"라고 답했는데 파일이 한 줄도
안 바뀌었다. 그 턴에 받은 도구 결과는 `read_file`·`search_text` 뿐이었고 둘 다
`ok:true` 였다. **읽기 성공을 쓰기 성공으로 오독한 것이다.**

**수정** — 커밋 `101d38a`, 오탐 제거는 `5aeb524`.
- `core/verification/apply_claim.py` 신규. 적용 계열 완료 주장을 잡고, user 메시지에
  실려 온 클라이언트 도구 결과를 파싱해, 성공한 도구가 **읽기 전용뿐이면** 근거
  없음으로 본다. "도구 결과가 있다"만 세면 이번 사고가 그대로 빠져나간다.
- 경고는 본문이 아니라 응답의 `warnings` 배열로 나간다. 플러그인 팀이 8/20 에
  요청한 신호가 이것이다 — "HTTP 200, finish_reason=stop, warnings 빈 배열이라
  정상 응답과 구분할 수 없다".
- **`final_proposal` 은 검사에서 제외한다.** 클라이언트가 승인 뒤 로컬에서
  적용하므로 정상 흐름에서도 서버는 쓰기 성공을 볼 수 없다. 제안은 완료 선언이
  아니다. 이 분기가 없어 경고 222건 중 153건(69%)이 오탐이었다. 정밀도 31% → 99%.

**차단이 아니라 경고다.** 실제로 막으려면 플러그인의 종료 게이트가 필요하다.

---

## 관측 계측 (2026-08-25 추가)

커밋 `4999db4`. `NEXUS_PROMPT_DUMP_DIR` 이 있을 때만 동작하며 기본은 꺼져 있다.

기존 `dump_prompt` 는 `model_provider.stream()` **직전**의 `system_prompt`·`messages`
만 찍었다. 그런데 `prompt_tokens` 를 좌우하는 것은 그 뒤 Tier 3 가 조립하는 payload
다 — `tools`, `tool_choice`, `response_format`, `chat_template_kwargs`. 08-24 조사가
정확히 이 사각지대에 막혔다.

- `dump_payload()` — httpx POST 직전의 payload 전문
- `dump_usage()` — 응답의 usage. **이게 없으면 덤프를 여러 개 회수해도 어느 것이
  문제의 요청인지 구분할 수 없다.**
- 키는 `ContextVar` 로 넘긴다. `inference.stream()` 이 `request_id` 를 인자로 받지
  않고, 모듈 전역에 두면 동시 요청끼리 섞인다(요청마다 세션 전용 엔진이 병렬로 돈다).
- `set_context` 는 `dump_prompt` 안에서 부른다. 호출부(`query_loop`)를 건드리지
  않기 위해서다 — 아래 배포 사고 참조.

---

## 확정된 관측 · 라우팅 편중

밤샘 9시간 회차(19,869 세션) 전수 집계다.

```
트래픽        qwen3-coder-30b 13,261 (66.7%)   ax-4.0 6,608 (33.3%)
거짓 완료 405건
   qwen3-coder-30b   405 (100%)   트래픽 비율대로면 기대 270건
   ax-4.0              0 (0%)     기대 135건인데 0건
```

**A.X-4.0 이 처리한 6,608 세션에서 거짓 완료가 단 한 건도 없다.** 코딩 모델 고유의
행동 특성으로 본다.

**그래도 라우팅은 건드리지 않는다.** 코딩 모델로 바꾼 이유가 코딩 품질이었고, 지금
통과율이 95.9% 로 최고치다. 한쪽 지표만 보고 되돌리면 다른 쪽이 무너진다.
플러그인 종료 게이트가 들어가면 모델과 무관하게 405건이 0이 되므로, **라우팅
조정은 마지막 수단**으로 남긴다.

---

## 배포 사고 기록 (2026-08-25 04:56, 12분 20초 장애)

부분 배포로 전 요청이 401 이 됐다. `health` 는 200 이라 겉으로는 정상이었다.

**원인.** 컨테이너가 브랜치보다 수백 줄 뒤처져 있었는데(`query_loop.py` 1,642 vs
1,801) 브랜치본을 그대로 복사했다. 구버전 `core/message.py` 에 없는
`STREAM_DISCARD_RETRY` 를 import 해 부트스트랩이 깨졌다.

**교훈 — 배포 규약**
1. **import 그래프는 전부 같은 리비전이거나 전부 옛 리비전이어야 한다.** 부분
   배포를 하려면 컨테이너의 실물 파일을 꺼내 거기에 변경만 얹는다.
2. **재시작 전에 컨테이너 안에서 import 검사를 돌린다.** 실패하면 재시작하지
   않는다 — 이 한 단계면 위 사고가 막혔다.
3. **`health` 200 을 정상 판정 근거로 쓰지 않는다.** 부트스트랩 완료 로그와
   **인증이 필요한 실제 요청**까지 확인한다.
4. 배포 로그 확인은 `--tail` 이 아니라 `--since` 로 한다. `--tail` 은 직전 부팅의
   성공 로그를 잘못 집는다(실제로 그렇게 오판했다).

이후 전체 동기화로 정리했다(221개 파일, 불일치 0). 컨테이너 재생성 시에는
`nexus-web:latest` 가 아니라 **현재 컨테이너를 `docker commit` 한 이미지**를 쓴다
— `docker cp` 로 덧입힌 내용이 이미지에 없다(773MB vs 713MB).

---

## 2026-08-26 · 코드 리뷰가 찾은 것 (에이전트 위임 첫 적용)

전역 규약의 "같은 컨텍스트에서 self-approve 금지"를 처음으로 적용해, 변경 후
`code-reviewer` 두 개를 병렬로 붙였다. **내가 "고쳤다"고 보고한 것을 두 번 정정했다.**

### 압축 — 08-25 수정은 증상의 절반만 고쳤다 (`12952ce`)

세션 격리는 **요청 간 누수**를 막았을 뿐이고, 같은 증상을 **요청 하나 안에서** 만드는
경로 둘이 그대로 살아 있었다.

- **C1** `_extract_recent_turns` 가 user 메시지 부족 시 빈 리스트를 돌려줬다. 주석에
  "호출 측이 이를 감안"이라 적혀 있었으나 감안하는 호출부가 하나도 없었다. 운영은
  `preserve_recent_turns=6` 인데 플러그인 요청은 user 가 1~2개다 → 큰 요청이 자기
  본문을 버리고 요약만 보냈다. **새 가드로도 못 막힌다** — 요약만 남았으니 "작아진
  것"은 사실이기 때문이다.
- **C2** `_compact_boundary` 는 교체 **전** 리스트의 인덱스인데 세 곳이 결과로 리스트를
  교체한다 → 다음 턴에 또 잘리고 요약이 두 번 붙는다. `mark_result_adopted()` 신설.

### 검증기 — 스트리밍에는 보호가 아예 없었다 (`d41017a`)

- **H1** `_openai_stream_generate` 에 `5aeb524` 의 분기가 없어 구조화 출력 본문이
  그대로 깨졌다. 게다가 `build_structured_warnings` 가 **미배선**이었다. 비스트림보다
  나쁘다 — 스트림은 되돌릴 수 없어 서버가 자기 실수를 발견할 기회조차 없다.
- **H2·H3** 봉투 파서가 문자열 안 중괄호를 세어, `log.info("start {")` 가 든 파일을
  읽으면 **27,710자에 642ms 정지 + 쓰기 증거 소실**. `raw_decode` 로 교체.
- **M1** deny-list → allow-list. `tool_request` 에도 같은 오탐이 있었다. 프로토콜의
  `VERIFYING` 이 쓰기 직후 검증 도구를 다시 요청하는 정상 경로다.
- **M2** 어간을 토큰 경계로, 그리고 **쓰기 우선**으로 뒤집었다. "읽기 우선"은 가상의
  이름(`read_file_and_apply`)을 막으려다 실재하는 편집 도구 5개를 읽기로 뒤집고 있었다.

### 배운 것

리뷰어 지적을 그대로 수용하지 않고 전부 코드·재현으로 확인했다. 실제로 M2 는
권고(토큰 경계)만으로는 안 풀려 규칙 자체(읽기 우선)를 뒤집어야 했다. 반대로 H2 는
균형 잡힌 중괄호로는 재현이 안 돼 처음엔 의심했는데, **불균형** 케이스에서 그대로
나왔다 — 재현을 못 했다고 기각하면 놓칠 뻔했다.

---

## 2026-08-26~27 · 나머지 결함 처리

### 붕괴 재생성이 절단본을 남겼다 (`f02cd98`)

`query_loop` 은 생성 붕괴를 감지하면 재생성하면서 `STREAM_DISCARD_RETRY` 신호를 낸다.
그 자리 주석이 "파이프 모드는 이 신호를 받으면 여태 모은 버퍼를 버려야 한다"고 명시하고,
심지어 "비스트리밍 소비자는 완전히 깨끗하게 교체된다"고 적혀 있었다. **그 문장이 사실이
아니었다** — 웹은 신호를 듣지 않았다. CLI 만 듣고 있었다.

```
06:51:14  절단 6,625자 + 재생성 255자 = 6,877자   ← 플러그인이 보고한 len 과 일치
07:42:11  절단 6,015자 + 재생성 254자 = 6,269자   ← 일치
```

모델은 정상 JSON 을 냈다(생성 90토큰=255자). **또 서버가 자기 응답을 망가뜨렸다.**
웹 소비 지점 세 곳에 배선했다. 스트림은 이미 보낸 delta 를 되돌릴 수 없어 신호를
`warnings` 로 전달해 클라이언트가 버리게 한다.

함께: 500 응답에 `request_id`·오류 코드를 싣는다. 스택 트레이스는 로그에만 남긴다.

### 격리 배선·OOM 복원·무효 압축 latch (`13ef2d8`)

- **격리 배선 테스트** — 기존 테스트는 `for_session()` 만 봤고 `web/app.py` 배선은
  안 봤다. 그 줄을 공유로 되돌려도 전부 통과했다. **뮤테이션으로 확인**했다 —
  되돌리면 정확히 두 건이 실패하고 원복하면 통과한다.
- **OOM 축소 복원** — `restore_max_tokens()` 를 요청 경계에서 부른다. 덤으로
  `for_session()` 이 **현재값**을 복제하던 결함도 찾았다(줄어든 상태에서 clone 하면
  축소가 새 세션까지 따라갔다). 어제 만든 코드의 버그고 테스트를 쓰다 걸렸다.
- **무효 압축 latch** — 무효였던 시점의 메시지 수를 기억해 대화가 늘기 전에는
  건너뛴다. `force` 는 무시한다(에러 복구 경로라 막으면 복구가 죽는다).

### 압축 실전 검증은 방식을 바꿨다

자연 발생을 기다리는 것은 현실성이 없다. **배포 후 9,978건 요청 동안 auto-compact 이
0건**이다 — 플러그인 요청이 10~20K 토큰이라 임계치 55,296 을 못 넘는다. 어제 압축은
다른 워크로드(웹 UI 긴 대화)에서 났다.

대신 배선을 뮤테이션 테스트로 못박았다. 그리고 배포 검증 중 실제로 무효 압축 경로가
로그에 찍혔다 — `1,307 → 11,842 토큰, 원본을 유지하고 압축 상태를 남기지 않는다`.
어제 경계를 남겨 입력을 잘라먹던 바로 그 경로다.

---

## 열려 있는 항목

| 항목 | 상태 |
|---|---|
| 플러그인 종료 게이트 | 명세 전달 대기 (`user_mig/design/PLUGIN_PROTOCOL_HARDENING_2026-08-25.md`) |
| 라우팅 조정 | 보류 — 405/405 편중은 확정. 종료 게이트 반영 후 재측정해 필요성 판단 |
| 압축 로직 품질 | 6회 중 정상 동작이 한 번도 없었다(2회는 토큰이 늘고 4회는 대화를 거의 전부 날렸다). C1·latch 로 완화됐을 수 있으나 재측정 필요 |
| `downloads` 주석 정정 | 구조화 출력에서는 표준 SDK 가 비표준 최상위 필드를 버리므로 "표준 클라이언트는 content 의 마크다운 링크를 본다"가 거짓이 됐다 |
| L3~L5 (사소) | `_MAX_QUOTES` 미사용, 구조화 출력에서 경고 인용문이 JSON 원문 통째로 나옴, `_FILE_MARKER_RE` 확장자 누락 |

---

## 참고 문서

- `user_mig/design/PLUGIN_PROTOCOL_HARDENING_2026-08-25.md` — 플러그인 팀 전달용
  프로토콜 강화 설계 명세(상태 머신·스키마 제한·오류 봉투·왕복 예산·선검증·종료 게이트).
  별도 파일로 둔 이유는 **다른 팀에 넘기는 산출물**이기 때문이다.
- `user_mig/nova_prompt_dump_2026-08-24/` — 시스템 프롬프트·도구 스키마 기준선과
  조사 기록. 원본 덤프는 사내 문서·고객 코드가 들어 있어 리포에 넣지 않았다.
