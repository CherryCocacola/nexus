"""오케스트레이터 패키지 — 쿼리 루프와 그 주변 모듈을 하나로 묶는 패키지 정의 파일.

이 패키지가 무슨 역할인지 (개요)
--------------------------------
Project Nexus의 두뇌에 해당하는 "에이전트 턴 루프"가 여기에 모여 있다.
사용자 입력이 들어오면 모델을 호출하고, 모델이 내놓은 스트림을 이벤트로 받아
도구를 실행하고, 그 결과를 다시 모델에게 돌려주는 과정을 도구 호출이 끝날
때까지 반복한다. 이 "반복" 자체가 오케스트레이터의 핵심이다.

4-Tier AsyncGenerator 체인에서의 위치
--------------------------------------
Nexus의 데이터 흐름은 4계층 AsyncGenerator 체인을 통과하며, 이 패키지는
그중 Tier 2(에이전트 턴 루프)를 담당한다. 각 계층은 아래로 갈수록 더 낮은
수준의 관심사를 처리하고, 이벤트(StreamEvent)는 하위 → 상위로 전파된다.

  - Tier 1: QueryEngine.submit_message()  — 세션 오케스트레이터(진입점)
  - Tier 2: query() / query_loop()        — while(True) 에이전트 턴 루프  ← 이 패키지
  - Tier 3: query_model_streaming()       — SSE 스트림 파싱
  - Tier 4: with_retry()                  — 재시도 + httpx 클라이언트

한 턴의 큰 그림은 "모델 호출 → 이벤트 수집 → 도구 실행 → 다음 턴"이며,
도구를 더 부를 필요가 없다고 판단되면 루프를 종료한다.

포함된 하위 모듈
----------------
  - query_loop:      while(True) 에이전트 턴 루프. 이 패키지의 심장부로,
                     한 턴을 돌리고 계속할지(ContinueReason) 끝낼지 결정한다.
  - stream_handler:  모델이 스트리밍하는 도중, 동시성이 안전한 도구를 병렬로
                     실행해 지연을 줄이는 스트리밍 실행 최적화 로직.
  - context_manager: 컨텍스트 창이 넘칠 때를 대비한 4단계 컨텍스트 압축
                     (compact) 파이프라인. 대화 이력을 요약/정리해 예산 안에 맞춘다.
  - stop_resolver:   모델 응답과 도구 사용 여부를 보고 "이번 턴에서 멈출지,
                     다음 턴으로 이어갈지"를 판정하는 종료/계속 판단 로직.

참고 (설계 규칙)
----------------
이 패키지는 4-Tier 체인을 절대 우회하지 않는다. 즉 Tier 2는 자기 하위인
Tier 3의 AsyncGenerator만 소비하고, Tier 4(with_retry)를 직접 호출하지 않는다.
또한 내부에서는 항상 OpenAI tool_calls 형식만 다루며, 모델별 XML 폴백 파싱
같은 세부는 하위 계층(ModelAdapter)에 캡슐화되어 이 루프에는 노출되지 않는다.

작성자: 이현수 / 작성일: 2026-07-05
"""
